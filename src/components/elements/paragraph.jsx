import React from "react";

export default function Paragraph({ className }) {
  return (
    <div
      className={`${className} py-1 max-w-52 w-full bg-gradient-to-r from-cyan to-pink`}
    ></div>
  );
}
